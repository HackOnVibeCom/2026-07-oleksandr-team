/**
 * Public Supabase config for client-side pages (install page, dashboard).
 *
 * Copy this file to `config.js` (gitignored) and fill in the real values.
 * These are PUBLIC values: the anon key is safe in the browser — it is
 * protected by Row Level Security on the database.
 */
window.GROWTHKIT_CONFIG = {
  supabaseUrl: "https://YOUR-PROJECT.supabase.co",
  supabaseAnonKey: "YOUR-ANON-PUBLIC-KEY",
};
